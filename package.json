{
  "name": "adcv-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npx playwright install chromium",
    "scrape": "node scripts/scrape-rffm.js",
    "build": "node scripts/scrape-rffm.js && eleventy",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "playwright": "^1.45.0"
  }
}
